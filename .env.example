# Canonical origin for metadata, canonical links and JSON-LD.
# Leave unset on Vercel preview deployments — the deployment URL is used automatically.
NEXT_PUBLIC_SITE_URL=https://kosmetikschule-picha.de
